<UserControl x:Class="KCK_APP.WPF.Views.ReservationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:KCK_APP.WPF.Views"
             xmlns:models="clr-namespace:KCK_APP.Models">
    <Grid Margin="10">
        <ListView x:Name="ReservationsList" Margin="0,0,0,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Marka" DisplayMemberBinding="{Binding CarBrand}" Width="120"/>
                    <GridViewColumn Header="Model" DisplayMemberBinding="{Binding CarModel}" Width="120"/>
                    <GridViewColumn Header="Data Startowa">
                        <GridViewColumn.DisplayMemberBinding>
                            <Binding Path="StartDate" StringFormat="dd.MM.yyyy" />
                        </GridViewColumn.DisplayMemberBinding>
                    </GridViewColumn>
                    <GridViewColumn Header="Data Końcowa">
                        <GridViewColumn.DisplayMemberBinding>
                            <Binding Path="EndDate" StringFormat="dd.MM.yyyy" />
                        </GridViewColumn.DisplayMemberBinding>
                    </GridViewColumn>
                    <GridViewColumn Header="Użytkownik" DisplayMemberBinding="{Binding Username}" Width="150"/>
                    <GridViewColumn Header="Akcje" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Usuń"
                                        Tag="{Binding Id}"
                                        Click="DeleteReservation_Click"
                                        Width="80"
                                        Margin="5,0,0,0"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
